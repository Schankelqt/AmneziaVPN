# Public hostname or IP that clients will use (must match reality)
WG_HOST=109.172.93.80

# wg-easy v15 requires bcrypt hash (not plain password).
# Generate with: docker run --rm ghcr.io/wg-easy/wg-easy wgpw 'your-strong-password'
WG_EASY_PASSWORD_HASH=replace_with_bcrypt_hash
